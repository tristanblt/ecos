#ifndef ECOS_HPP_
#define ECOS_HPP_

namespace ecs {
    class Ecos {
        public:
            Ecos() = default;
            ~Ecos() = default;

            void run();

            // void addEntity();
            // void addSystem();

        private:
    };
}

#endif /* !ECOS_HPP_ */
